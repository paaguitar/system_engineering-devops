#!/usr/bin/env bash
# This script lists filenames in the current directory after the first dash

for file in *; do
  # Skip if not a file or doesn’t contain a dash
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
